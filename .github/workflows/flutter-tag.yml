name: Flutter Tag Build

on:
  workflow_dispatch:
  push:
    tags:
       - '1.2.3'  # 监听推送的标签，确保它与 `1.2.3` 相匹配

jobs:
  run-flutter-tag-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: ${{ github.ref_name }}
